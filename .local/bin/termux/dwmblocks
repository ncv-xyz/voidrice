#!/bin/sh

# dwmblocks wrapper for Termux

if [ -n "$SSH_CLIENT" ] || [ -n "$SSH_TTY" ]; then
	modules() {
		# sb-kbselect
		#cat "$PREFIX"/tmp/recordingicon 2>/dev/null
		sb-tasks
		# sb-music
		sb-pacpackages
		sb-news
		# sb-price xmr-btc "Monero to Bitcoin" 🔒 25
		# sb-price xmr Monero 🔒 24
		# sb-price eth Ethereum 🍸 23
		# sb-price btc Bitcoin 💰 21
		sb-torrent
		# sb-memory
		# sb-cpu
		# sb-moonphase
		sb-doppler
		sb-forecast
		sb-mailbox
		#sb-nettraf
		sb-volume
		sb-battery
		sb-clock
		sb-internet
		# sb-iplocate
		sb-help-icon
		}
	status() {
		printf "%s\n" "$(modules | tr "\\n" " ")" | tr -s " "
	}
	while :; do
		status 2>/dev/null
		sleep 1m &
		wait
	done
else
	"$PREFIX"/bin/dwmblocks "$@"
fi
