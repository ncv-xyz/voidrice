#!/bin/sh

printf "Beginning upgrade.\n"

sudo pacman -Syu

command -v yay >/dev/null 2>&1 && yay -Su

pkill -RTMIN+8 "${STATUSBAR:-dwmblocks}"

printf "\nUpgrade complete.\nPress any key to exit window."
read -r _
